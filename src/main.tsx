import React from 'react'
import ReactDOM from 'react-dom/client'
import TabPage from './NewTabPage.tsx'
import './index.scss'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TabPage />
  </React.StrictMode>
)
